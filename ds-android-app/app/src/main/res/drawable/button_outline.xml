<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#1Af5f5f5"/> <!-- 10% white fill when pressed -->
            <stroke android:width="1.5dp" android:color="@color/off_white"/>
            <corners android:radius="20dp"/>
        </shape>
    </item>
    
    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="@android:color/transparent"/>
            <stroke android:width="1.5dp" android:color="@color/gray_medium"/>
            <corners android:radius="20dp"/>
        </shape>
    </item>
</selector>